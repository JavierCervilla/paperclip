# 2026-03-19

## Chat with board user

- Reviewed all active issues. Found 5 duplicates (PAP-30, PAP-31, PAP-33, PAP-34) of PAP-29 plus a test issue (PAP-32). Cancelled all duplicates, kept PAP-29.
- Discussed release/deploy process. Confirmed this is a fork — master syncs from upstream, no npm releases here.
- Learned Dokploy has two environments: preview (staging, branch `preview`) and production (branch `deploy/dokploy`).
- Flow: upstream/master → origin/master → preview (staging) → deploy/dokploy (production).
- Identified gap: no CI workflows for `preview` or `deploy/dokploy` branches.
